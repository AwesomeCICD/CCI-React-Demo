test('calculates 1 + 1', () => {
  let math = 1 + 1;
  expect(math).toEqual(2);
});

test('calculates 1 - 1', () => {
  let math = 1 - 1;
  expect(math).toEqual(1);
});

test('calculates 1 + 2', () => {
  let math = 1 + 2;
  expect(math).toEqual(3);
});

test('calculates 1 - 2', () => {
  let math = 1 - 2;
  expect(math).toEqual(-1);
});

test('calculates 1 + 3', () => {
  let math = 1 + 3;
  expect(math).toEqual(4);
});

test('calculates 1 - 3', () => {
  let math = 1 - 3;
  expect(math).toEqual(-2);
});

test('calculates 1 + 4', () => {
  let math = 1 + 4;
  expect(math).toEqual(5);
});

test('calculates 1 - 4', () => {
  let math = 1 - 4;
  expect(math).toEqual(-3);
});

test('calculates 1 + 5', () => {
  let math = 1 + 5;
  expect(math).toEqual(6);
});

test('calculates 1 - 5', () => {
  let math = 1 - 5;
  expect(math).toEqual(-4);
});

test('calculates 1 + 6', () => {
  let math = 1 + 6;
  expect(math).toEqual(7);
});

test('calculates 1 - 6', () => {
  let math = 1 - 6;
  expect(math).toEqual(-5);
});

test('calculates 1 + 7', () => {
  let math = 1 + 7;
  expect(math).toEqual(8);
});

test('calculates 1 - 7', () => {
  let math = 1 - 7;
  expect(math).toEqual(-6);
});

test('calculates 1 + 8', () => {
  let math = 1 + 8;
  expect(math).toEqual(9);
});

test('calculates 1 - 8', () => {
  let math = 1 - 8;
  expect(math).toEqual(-7);
});

test('calculates 1 + 9', () => {
  let math = 1 + 9;
  expect(math).toEqual(10);
});

test('calculates 1 - 9', () => {
  let math = 1 - 9;
  expect(math).toEqual(-8);
});

test('calculates 2 + 1', () => {
  let math = 2 + 1;
  expect(math).toEqual(3);
});

test('calculates 2 - 1', () => {
  let math = 2 - 1;
  expect(math).toEqual(1);
});

test('calculates 2 + 2', () => {
  let math = 2 + 2;
  expect(math).toEqual(4);
});

test('calculates 2 - 2', () => {
  let math = 2 - 2;
  expect(math).toEqual(0);
});

test('calculates 2 + 3', () => {
  let math = 2 + 3;
  expect(math).toEqual(5);
});

test('calculates 2 - 3', () => {
  let math = 2 - 3;
  expect(math).toEqual(-1);
});

test('calculates 2 + 4', () => {
  let math = 2 + 4;
  expect(math).toEqual(6);
});

test('calculates 2 - 4', () => {
  let math = 2 - 4;
  expect(math).toEqual(-2);
});

test('calculates 2 + 5', () => {
  let math = 2 + 5;
  expect(math).toEqual(7);
});

test('calculates 2 - 5', () => {
  let math = 2 - 5;
  expect(math).toEqual(-3);
});

test('calculates 2 + 6', () => {
  let math = 2 + 6;
  expect(math).toEqual(8);
});

test('calculates 2 - 6', () => {
  let math = 2 - 6;
  expect(math).toEqual(-4);
});

test('calculates 2 + 7', () => {
  let math = 2 + 7;
  expect(math).toEqual(9);
});

test('calculates 2 - 7', () => {
  let math = 2 - 7;
  expect(math).toEqual(-5);
});

test('calculates 2 + 8', () => {
  let math = 2 + 8;
  expect(math).toEqual(10);
});

test('calculates 2 - 8', () => {
  let math = 2 - 8;
  expect(math).toEqual(-6);
});

test('calculates 2 + 9', () => {
  let math = 2 + 9;
  expect(math).toEqual(11);
});

test('calculates 2 - 9', () => {
  let math = 2 - 9;
  expect(math).toEqual(-7);
});

test('calculates 3 + 1', () => {
  let math = 3 + 1;
  expect(math).toEqual(4);
});

test('calculates 3 - 1', () => {
  let math = 3 - 1;
  expect(math).toEqual(2);
});

test('calculates 3 + 2', () => {
  let math = 3 + 2;
  expect(math).toEqual(5);
});

test('calculates 3 - 2', () => {
  let math = 3 - 2;
  expect(math).toEqual(1);
});

test('calculates 3 + 3', () => {
  let math = 3 + 3;
  expect(math).toEqual(6);
});

test('calculates 3 - 3', () => {
  let math = 3 - 3;
  expect(math).toEqual(0);
});

test('calculates 3 + 4', () => {
  let math = 3 + 4;
  expect(math).toEqual(7);
});

test('calculates 3 - 4', () => {
  let math = 3 - 4;
  expect(math).toEqual(-1);
});

test('calculates 3 + 5', () => {
  let math = 3 + 5;
  expect(math).toEqual(8);
});

test('calculates 3 - 5', () => {
  let math = 3 - 5;
  expect(math).toEqual(-2);
});

test('calculates 3 + 6', () => {
  let math = 3 + 6;
  expect(math).toEqual(9);
});

test('calculates 3 - 6', () => {
  let math = 3 - 6;
  expect(math).toEqual(-3);
});

test('calculates 3 + 7', () => {
  let math = 3 + 7;
  expect(math).toEqual(10);
});

test('calculates 3 - 7', () => {
  let math = 3 - 7;
  expect(math).toEqual(-4);
});

test('calculates 3 + 8', () => {
  let math = 3 + 8;
  expect(math).toEqual(11);
});

test('calculates 3 - 8', () => {
  let math = 3 - 8;
  expect(math).toEqual(-5);
});

test('calculates 3 + 9', () => {
  let math = 3 + 9;
  expect(math).toEqual(12);
});

test('calculates 3 - 9', () => {
  let math = 3 - 9;
  expect(math).toEqual(-6);
});
